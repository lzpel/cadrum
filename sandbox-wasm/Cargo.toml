[package]
name = "wasm-experiment"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "*"
cadrum = { path = "..", optional = true }

[features]
cadrum = ["dep:cadrum"]
cpp = []
pure = []
