//! User-facing Color API for shapes (available when `feature = "color"` is enabled).
//!
//! This module exposes color-related functions for `Shape`. The actual methods
//! are implemented directly on the `Shape` struct in `shape.rs`.

// Currently all methods are on `Shape` directly. This module is reserved
// for any future free functions, traits, or color-specific utilities.
